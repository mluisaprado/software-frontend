name: Web CI/CD (Expo Web → Netlify)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  EXPO_PUBLIC_API_URL: https://software-backend-011n.onrender.com

jobs:
  lint:
    name: 🔍 Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.4
          cache: npm
      - name: Instalar dependencias
        run: npm ci --legacy-peer-deps
      - name: Ejecutar lint
        run: npm run lint || echo "No hay script de lint"

  test:
    name: 🧪 Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.4
          cache: npm
      - name: Instalar dependencias
        run: npm ci --legacy-peer-deps
      - name: Ejecutar tests
        run: npm test -- --coverage --coverageReporters=text --coverageReporters=lcov
      - name: Subir reporte de cobertura
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
        continue-on-error: true

  build:
    name: 🏗️ Build Web (Expo Metro)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.4
          cache: npm

      - name: Instalar dependencias
        run: npm ci --legacy-peer-deps

      - name: Crear config de Expo Router (para CI)
        run: |
          echo "export default { static: false };" > expo-router.config.js

      - name: Export web (Expo Metro)
        run: npm run build:web
        env:
          EXPO_PUBLIC_API_URL: ${{ env.EXPO_PUBLIC_API_URL }}

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

  deploy:
    name: 🚀 Deploy a Netlify (Producción)
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Para obtener historial completo
          
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.4

      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Instalar Netlify CLI
        run: npm install -g netlify-cli

      - name: Deploy con Netlify CLI
        run: |
          netlify deploy --dir=dist --prod
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Mostrar URL final
        run: echo "✅ Sitio publicado en https://santiago-software.netlify.app"

      - name: Crear tag y release automático
        id: create_release
        run: |
          # Generar versión basada en fecha
          VERSION="v$(date +'%Y.%m.%d')-$(date +'%H%M')"
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          
          # Generar notas de release
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            COMMITS=$(git log --pretty=format:"- %s (%h)" -10)
          else
            COMMITS=$(git log $LAST_TAG..HEAD --pretty=format:"- %s (%h)")
          fi
          
          # Crear release con tag (gh release create hace ambas cosas)
          gh release create $VERSION \
            --title "Release $VERSION" \
            --notes "## 🚀 Cambios en esta versión

          $COMMITS

          **Deploy Web**: https://santiago-software.netlify.app
          **Fecha**: $(date +'%Y-%m-%d %H:%M:%S')"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy-preview:
    name: 🔁 Deploy Preview a Netlify (PR / Develop)
    runs-on: ubuntu-latest
    needs: build
    if: github.ref != 'refs/heads/main' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.4

      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Instalar Netlify CLI
        run: npm install -g netlify-cli

      - name: Deploy preview con Netlify CLI
        run: |
          netlify deploy --dir=dist
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          
